# 资质智能审核助手 - 环境变量配置模板
# 复制此文件为 .env 并填入你自己的配置
#
# ⚠️ 最省事：不用手填。把这文件交给 AI，说「帮我检查并修复资质审核配置」，
#    AI 跑 doctor 自动填好大部分（发卡群/路径/脚本）。你只做 2 个选择：
#    ① 岗位(法人岗/非法人岗) ② 环境(练手 test / 正式 prod)——首跑 list 时 AI 会弹卡片让你选。

FEISHU_APP_ID=<你的飞书应用ID>
QUAL_DEFINITION_CODE=<审批定义code>
FEISHU_USER_OPEN_ID=<审批人open_id>
QUAL_ATTACH_DIR=<附件缓存目录，如 D:\fando-ocr-cache>
LARK_AUDIT_CHAT_ID=<审核结果发卡群chat_id —— 让 AI 设为“你当前这个群”的 chat_id；不设则拒发，绝不误发到别人的群>
# QUAL_TEST_CHAT_ID：可选。test 环境专用发卡群。不设则 test 也发到上面的 LARK_AUDIT_CHAT_ID。
#   多数团队不用设（test/prod 同群即可）；只有想把 test 卡与 prod 卡分到不同群时才设。
# QUAL_TEST_CHAT_ID=
QUAL_SOFFICE_BIN=<LibreOffice路径，如 D:\Program\soffice.exe>  # 可选
# QUAL_CARD_SCRIPT 无需配置：代码自动定位 scripts/gen_card_from_json.ps1（除非放到非标准路径才设）
# ── 环境（test / prod）──
# QUAL_PROFILE=test（默认）：🧪 练手环境——能走完 list→审核→出卡（卡就发到你上面配的 LARK_AUDIT_CHAT_ID 群），
#   但【不能做真实审批】(点 F/A/I/R 会被拦)，台账也隔离。适合先把流程跑通、在自己群里看到卡确认没问题。
# QUAL_PROFILE=prod：🔴 正式环境——可做真实审批、真执行。流程验证 OK 后，让 AI 帮你切 prod。
#   不管 test/prod，卡片都发到 LARK_AUDIT_CHAT_ID 指定的群（群路由与审批开关是分开的）。
# QUAL_PROFILE=test
# 🔴 角色（faren=法人岗 / feifaren=非法人岗）：【首次故意留空】→ 首跑 list 会弹卡片让你选岗位、自动写回这里。
#    别在这里预设，否则跳过选岗流程。选好后此处会变成 QUAL_AUDIT_ROLE=faren 或 feifaren。
# QUAL_AUDIT_ROLE=
