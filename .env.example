# 资质智能审核助手 - 环境变量配置示例
# 复制此文件为 .env 并填入实际值，或直接在系统中设置环境变量
# 完整参数说明见 references/audit-tool-ref.md

# ──────────────────────────────────────────────
# ⚠️  运行环境（最重要）：影响发卡目标群 + approve 是否放行
#   prod = 生产（发生产群 oc_231f，允许真实审批动作）
#   test = 测试（发测试群 oc_e819，物理禁止真实审批）← 默认，忘设=安全
# ──────────────────────────────────────────────
QUAL_PROFILE=test

# 飞书应用ID（用于生成审批链接）
FEISHU_APP_ID=cli_9cb844403dbb9108

# 资质申请审批定义码
QUAL_DEFINITION_CODE=0E0BBB7F-A4C8-471F-8051-3E4E88A83856

# 执行用户 open_id（写/查审批评论用）。
# 必须是 lark-cli 默认根授权的那个用户 = 大公子桥 app cli_aaa274a26fba9cca 的用户(王伊瑄)；
# 用别的 app 的 open_id 会报 99992361 open_id cross app。审批评论接口只收 bot/tenant token + 本 app 的 user_id。
FEISHU_USER_OPEN_ID=ou_102cae80079463e6c8281777fec96f47

# 附件下载 + OCR 结果缓存目录（独立于系统盘，便于清理；可挂定时清理旧案目录）
QUAL_ATTACH_DIR=D:\fando-ocr-cache

# Python 解释器（readPDF 的 PyMuPDF 文本层检测 + readDOCX 的 python-docx 用）
# 必须指向含 PyMuPDF / python-docx / paddleocr 依赖的虚拟环境 python
QUAL_PYTHON_BIN=python

# ocr-paddle 的 CLI 入口（图片/扫描PDF 走它）。留空则按 __dirname 自动解析到 ../ocr-paddle/scripts/ocr.cjs
# QUAL_OCR_CLI=C:\Users\FD\AppData\Roaming\FanDo\openclaw\skills\ocr-paddle\scripts\ocr.cjs

# LibreOffice soffice.exe（.doc → docx、非标DOCX → pdf 转换）。
# 2026-07-13 起可选：不设此项时会自动探测常见安装目录（Program Files / /usr/bin 等）+ PATH，
# 找不到才降级为 .doc/非标DOCX 转人工（不会报错崩溃）。装在非常见路径时才需要显式指定，如本机：
QUAL_SOFFICE_BIN=D:\软件\program\soffice.exe

# LibreOffice 用户配置目录（隔离多进程冲突，与 QUAL_SOFFICE_BIN 配套使用）。
# 2026-07-13 起可选：不设此项时默认落系统临时目录（os.tmpdir()/fando_lo_profile），自动建目录，无需手工建 C:\temp。
QUAL_LO_PROFILE=file:///C:/temp/fando_lo_profile

# 卡片生成脚本路径（不在 skill 包内，按 gen-card 子命令读取）
# 标准部署（skill 在 .../skills/qualification-audit/，脚本在 .../scripts/）时可不设——工具会自动探测 ../../scripts/gen_card_from_json.ps1
# 非标准路径时才需要显式指定。更换机器后只改此变量，SKILL.md 不用动。
# QUAL_CARD_SCRIPT=D:\agent-hub\scripts\gen_card_from_json.ps1
QUAL_CARD_SCRIPT=C:\claude\gen_card_from_json.ps1

# 日期窗：list 命令默认只审近 N 天提交的待办；0 或不设 = 全量（清库存用）
# QUAL_SINCE_DAYS=7

# 单轮总超时（秒）：过此时间仍 pending 的件标 timeout 自动放行发卡，建议 ≤1800
# QUAL_ROUND_TIMEOUT=900

# （可选，C7）审核触发来源/执行身份归因。设了就会在 AI 评论末尾留一行"审核触发来源：xxx"，便于审计。
# 例：agent-2/cron 或 大公子桥/手动。注意 FEISHU_USER_OPEN_ID 仍须与 lark-cli 实际 bot 身份匹配，否则评论 API 跨 app 失败(99992361)。
# QUAL_TRIGGER_SOURCE=agent-2/cron
