# 测试策略（分层 + 场景 → 用什么测）

> 业务锚点：**Skill 规则迭代 / 优化上线，防止功能与规则退化**。本文把「我们现有的测试资产」映射到通用的 Mock / CI闸门 / 冒烟 / 回归 / T1–T4 框架，并给出「什么场景用什么测」。**已有 vs 待建分开标注**（不把蓝图当现状）。

## 一、我们现有的测试家底

| 资产 | 类型 | 特征 | 状态 |
|---|---|---|---|
| `golden_tests/runner.cjs`（T1–T5，131 用例） | **回归测试（Mock 实现）** | 目的是**防退化**(改动后重跑历史用例)=回归；实现上直接 `require` 目标函数、**不起进程/不碰飞书 API**=Mock。秒级；T1=FAIR 守卫 / T2=gen-card·spawn 模板 / T3=golden_flow / T4=结构·SKILL.md 行数闸 / T5=判据一致性 | ✅ 现有 |
| `scripts/smoke.cjs`（7 项） | **冒烟 / BVT** | **真起子进程** `node audit-tool.cjs …` 跑核心链路；`QUAL_PROFILE=test` 隔离台账+禁真 approve；**只判代码级崩溃**（ReferenceError/语法/编码/非受控退出），飞书网络错不算；**发 tag 前必绿（已入棘轮）** | ✅ 现有 |
| `scripts/card-live-test.cjs` | 真链路点测 | 真发卡 POST-vs-PATCH（活卡幂等） | ✅ 现有 |
| `batch-t2-eval / gen-eval-tasks / compare-eval-results` | **AI 判断一致性评测**（agent-eval） | 跑子代理判断、比对结论 | 🟡 有工具，基线未固化 |
| `node --check` | 静态语法 | 秒级 | ✅ 现有 |

## 二、映射到通用框架（Mock / CI / 冒烟 / 回归 / T1–T4）

| 阶段 | 目的 | 用什么 | 我们的资产 | Mock? | 时长 |
|---|---|---|---|---|---|
| **T1 本地自测** | 新增/优化规则，快速验证 | 静态 + Mock 回归 | `node --check` + `golden_tests/runner.cjs` | 重度 Mock | 秒级 |
| **CI 闸门（准入）** | 拦致命故障、代码能不能合并 | 静态 + Mock 回归 + **冒烟** | `node --check` + golden runner + `smoke.cjs` | Mock+轻量真进程 | **目标 3–10min** |
| **T3 测试环境（回归主场）** | 业务逻辑有没有被改坏、防退化 | **真实上下游回归** | `QUAL_PROFILE=test` 对真实审批跑 list/case + **真机对比改动前后 person/quals/scope 判定** + eval 一致性 + card-live-test | **无 Mock** | 慢（后置） |
| **T4 预发（上线前最后一关）** | 生产同款环境跑得稳、结果对、无隐性退化 | prod-1:1、真实样本回放/抽样冒烟 | prod profile 影子跑 / 真实样本抽样 | 无 Mock | 按风险 |

> **⚠️ 两个维度别混（golden 到底是 Mock 还是回归？）**：**Mock↔真链路**说的是"依赖怎么处理"；**回归↔冒烟**说的是"测试目的"——两轴正交、不互斥。**golden 本质是回归**（目的=防退化、重跑历史用例），只是**用 Mock 实现**（require 直调、隔离飞书 API，所以快）。所以：**回归按实现分两种——① Mock 回归(golden，T1/CI，快) ② 真链路回归(T3，真上下游，慢·后置)**；golden 是①，不是"不算回归"。
>
> **关键取舍（遵你的结论）**：**CI 不放全量真链路回归**（慢、阻塞迭代）；我们的 golden 是 Mock 回归、秒级，可进 CI；**真链路全量回归后置到 T3**。冒烟(smoke) ≠ 回归(golden)：冒烟只看"活不活"，回归看"老逻辑有没有坏"。

## 三、按业务场景对号入座（什么改动 → 测到什么程度）

| 场景 | 测什么 | 用什么 | 到哪一层 |
|---|---|---|---|
| **微小规则调整** | 没弄坏老逻辑 + 能跑起来 | golden(mock) + smoke | CI 绿即可；T4 抽样冒烟 |
| **重大规则改版** | 全面防退化 + 真结果对 | golden+smoke(CI) → **T3 真数据回归 + eval 一致性** → T4 大样本回放 | 全链路 |
| **引擎/骨架改动**（如配置包重构、connector 改动） | 不可动骨架必须零回归 | golden(全绿) + smoke + **真机对比外置前后逐条一致** | T3 → T4 |
| **新团队接入（config pack）** | 该团队配置正确、判定符合预期 | 该团队 `fixtures/` golden + 真机对比 + **doctor 配置包体检** | T1 → T3 |

## 四、推荐流水线（CI/CD）

1. **T1 本地**：`node --check` + golden(mock) 验新规则。
2. **提交 → CI 闸门**：静态 + golden(mock 回归) + smoke(BVT)；失败**直接拦截、禁合并**。
3. **合主干 → T3 测试环境**：真实上下游，跑真数据回归 + eval 一致性，**防退化主场**。
4. **T3 过 → T4 预发**：prod 同款配置，抽样冒烟 / 样本回放（改动风险越高、验证越重）。
5. **T4 过 → 灰度 → 上线**。

## 五、责任边界（一句话记）

- **CI 闸门**：管"代码能不能合并"，拦低级致命 bug。
- **T3**：管"业务逻辑有没有坏"，回归主场、防退化。
- **T4**：管"放到生产同款环境能否稳定正确"，消除环境鸿沟。

## 六、现状 vs 待建（诚实标注）

- **✅ 现在就有**：golden mock 回归(131)、smoke 冒烟(发 tag 前必绿·已入棘轮)、card-live-test、node --check、`QUAL_PROFILE=test` 隔离测试(禁真 approve)。
- **⏳ 还要建**：
  - **CI 流水线自动化**——现在全靠**手动跑**（`.github` 里只有 CODEOWNERS、**无 workflow**），要把"静态+golden+smoke"接进 CI 成"合并准入闸"。
  - **T3 真数据回归的自动化**——现在是手动对比，要脚本化"取真实待办样本→跑→比对改动前后判定"。
  - **T4 预发环境**——现无独立 T4，暂用 prod profile 影子/抽样替代；未来建 1:1 复刻。
  - **eval 一致性基线（agent-eval）**——AI 判断层的回归基线未固化（promptfoo/DeepEval 待接）。

## 七、取舍原则（落地参考）

1. Mock 是本地&CI 提速利器，**不能替代真链路**（过度 Mock → "测试过、线上崩"）。
2. CI 坚持轻量，**禁塞耗时的全量真链路回归**（后置 T3）。
3. 重大规则改版：T4 必做大样本；微调仅 T4 抽样冒烟。
4. **发 tag 前 smoke 必绿**（已入棘轮，不可跳）。
