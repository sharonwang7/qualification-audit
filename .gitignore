# 数据卫生（F11/C5/C6）：以下为运行产物/真实数据/凭证，绝不随 skill 包分发或入库。
# 注意：本文件只阻止"分发/提交"时携带；已存在的同名文件需另行清理（见 SKILL 维护说明）。

# 临时运行产物（含真实表单/评论原文，崩溃残留）
scratch/
scratchpad/
_at_*.json
_p_*.json
_b_*.json
_d_*.json
_api_p_*.json
_card_from_json.json
*.lock
*.tmp

# 本机配置与凭证（仅 .env.example 入库，占位值）
.env

# 真实审核报告/状态机（若曾误落包内；正常应在 D:\agent-hub 或 QUAL_AUDIT_DIR 下）
audit_reports/
pending_actions.json

# OCR/附件缓存（若曾误落包内）
fando-ocr-cache/

# 测试/回归运行残留
examples/golden_set/**/case.json
